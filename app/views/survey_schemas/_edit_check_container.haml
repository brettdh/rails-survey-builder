.option_container
  %span.prompt= field.prompt
  %br
  - field.subfields.each do |subfield|
    %input{:type => :checkbox, :name => "field#{field.id}", :value => subfield.prompt}
      = render_field(subfield)
      = delete_subfield_link(subfield)
      %br
  = add_subfield_link(field)